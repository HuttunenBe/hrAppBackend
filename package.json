{
  "name": "hr-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3005"
  },
  "dependencies": {
     "json-server": "^1.0.0-beta.3"
  }
}
